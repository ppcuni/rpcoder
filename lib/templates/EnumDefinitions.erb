/* generated by rpcoder <%= version %> */
using System;
using System.Collections.Generic;

namespace <%= name_space %>
{
	<%- enums.each do |enum| -%>
	<%- if enum.description -%>
	/// <summary>
	/// <%= enum.description %>
	/// </summary>
	<%- end -%>
	<%- if enum.flags? -%>
	[Flags]
	<%- end -%>
	public enum <%= enum.name.to_s.camelize %>
	{
		<%- enum.constants.each do |i| -%>
		<%- if i.description -%>
		/// <summary>
		/// <%= i.description %>
		/// </summary>
		<%- end -%>
		<%= i.name.to_s.camelize %> = <%= i.num %>,
		<%- end -%>
	}

	<%- end -%>
	public static class EnumUtility
	{
	<%- enums.each do |enum| -%>
		public static IEnumerable<<%= enum.name.to_s.camelize %>> <%= enum.name.to_s.camelize %>Values
		{
			get
			{
				<%- enum.constants.each do |i| -%>
				yield return <%= enum.name.to_s.camelize %>.<%= i.name.to_s.camelize %>;
				<%- end -%>
			}
		}

	<%- end -%>
	}
}
